<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/delConfirmationBottomSheet"
    android:layout_width="match_parent"
    android:layout_height="120dp"
    android:clickable="true"
    android:background="@drawable/nowplaying_card_shape"
    android:backgroundTint="@color/blackAlpha">
    <TextView
        android:id="@+id/bottomConfirmDeleteTitle"
        android:layout_width="match_parent"
        android:layout_height="40dp"
        android:layout_marginStart="20dp"
        android:layout_marginEnd="20dp"
        android:textColor="@color/alphabet_background_color"
        android:gravity="start|center"
        android:layout_alignParentTop="true"
        />
    <TextView
        android:id="@+id/bottomConfirmText"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/bottomConfirmDeleteTitle"
        android:textColor="@color/alphabet_background_color"
        android:layout_marginStart="20dp"
        android:layout_marginEnd="20dp"
        />

    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="40dp"
        android:layout_alignParentEnd="true"
        android:layout_alignParentBottom="true">

        <Button
            android:id="@+id/bottomDialogCancel"
            android:layout_width="wrap_content"
            android:layout_height="match_parent"
            android:text="Cancel"/>
        <Button
            android:id="@+id/bottomDialogDelete"
            android:layout_width="wrap_content"
            android:layout_height="match_parent"
            android:text="Delete"/>
        <Button
            android:id="@+id/bottomDialogRemove"
            android:layout_width="wrap_content"
            android:layout_height="match_parent"
            android:visibility="gone"
            android:text="Remove"/>

    </LinearLayout>

</RelativeLayout>